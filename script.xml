Simulation.addWatchLists([{name:"vars",variablePaths:["example1.x", "example1.y"]}]);

Simulation.startWatch();

G.addWidget(Widgets.PLOT);

var options = {yaxis:{min:-30,max:30},xaxis:{min:-30,max:30,show:false}};

Plot1.setOptions(options)

Plot1.setName("The butterfly effect");

Plot1.setPosition(140,140);

Plot1.setSize(600,900);

Simulation.start();

Plot1.plotXYData(x,y);

